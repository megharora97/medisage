import { StyleSheet } from "react-native";
import { normalize } from "../../utils/helper";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    main:{ width: '100%', height: normalize(70) }
})